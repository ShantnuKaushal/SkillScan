# Data Directory

This directory is reserved for local development data and generated samples.

The contents of `data/raw/` and `data/samples/` are ignored by git so large or environment-specific files do not get committed.
